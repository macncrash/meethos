{
  "name": "meethos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "meethos / earth3 — a living simulation game from galaxy to civilization, one continuous zoom.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:single": "tsc --noEmit && vite build -c vite.config.singlefile.ts",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "satellite.js": "5.0.0",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@types/three": "^0.184.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
